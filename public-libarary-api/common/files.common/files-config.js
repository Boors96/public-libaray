
const _ = require('../env.common/env');

// exports.uploadFile = (file) => {
//     return file.mv(_.TARGET_PATH + file.name).then((fileResult) => {
//         return setFilePath(server_url, file.name);
//     }).catch((err) => { return err; });
// };

// exports.setFilePath = (file) => {
//     return  _.FILE_PATH + file.name;
// };